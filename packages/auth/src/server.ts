/**
 * Server-side helpers shared by both apps (session validation, cookie
 * names, hooks). The auth instances themselves come from the factories:
 * `@openbookings/auth/guest` and `@openbookings/auth/host`.
 */
export * from "./shared";
